<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Products</title>
    <!-- Semantic UI CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.5.0/semantic.min.css">
    <style>
        .red-text {
            color: red;
        }
        .blue-text {
            color: blue;
        }
        .green-text {
            color: green;
        }
        /* Add more color classes as needed */
    </style>
</head>
<body>
<h1 style="text-align: center;">Product List</h1>
<div class="ui container">
    <table class="ui celled table">
        <thead>
        <tr>
            <th>ID</th>
            <th>Product Name</th>
            <th>Product Price</th>
        </tr>
        </thead>
        <tbody>
        {{#products}}
            <tr>
                <td class="red-text">{{id}}</td>
                <td class="blue-text">{{productName}}</td>
                <td class="green-text">{{productPrice}}</td>
            </tr>
        {{/products}}
        </tbody>
    </table>

    <form class="ui form" action="/products" method="post">
        <div class="field">
            <label>Product ID</label>
            <input type="text" name="id" placeholder="Product ID">
        </div>
        <div class="field">
            <label>Product Name</label>
            <input type="text" name="productName" placeholder="Product Name">
        </div>
        <div class="field">
            <label>Product Price</label>
            <input type="text" name="productPrice" placeholder="Product Price">
        </div>
        <button class="ui button" type="submit">Submit</button>
    </form>
</div>
</body>
</html>
